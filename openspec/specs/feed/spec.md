# Feed Specification

> Generated by spec-gen v1.0.0 on 2026-04-24 22:59

## Purpose

Manages the lifecycle of podcast episodes, including fetching artifacts, processing them into MP3
files, maintaining a history of episodes, purging old episodes, and rebuilding the podcast feed.

## Requirements

> `bridge/harvester.py`

### Requirement: LoadHistory

> Implementation: `load_history` in `bridge/harvester.py` · confidence: llm

The system SHALL loads the history of processed episodes from a json file.

#### Scenario: HistoryFileExists
- **GIVEN** History file exists at HISTORY_PATH
- **WHEN** loadHistory is called
- **THEN** History dictionary is returned

#### Scenario: HistoryFileDoesNotExist
- **GIVEN** History file does not exist at HISTORY_PATH
- **WHEN** loadHistory is called
- **THEN** Empty dictionary is returned

### Requirement: SaveHistory

> Implementation: `save_history` in `bridge/harvester.py` · confidence: llm

The system SHALL saves the history of processed episodes to a json file.

#### Scenario: HistoryFileIsSavedSuccessfully
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid history dictionary
- **WHEN** saveHistory is called
- **THEN** History is saved to HISTORY_PATH

### Requirement: ConvertToMp3

> Implementation: `convert_to_mp3` in `bridge/harvester.py` · confidence: llm

The system SHALL converts a wav file to an mp3 file using ffmpeg.

#### Scenario: ConversionSucceeds
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid WAV file path and MP3 file path
- **WHEN** convertToMp3 is called
- **THEN** MP3 file is created at the specified path

#### Scenario: ConversionFails
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Invalid WAV file path or MP3 file path
- **WHEN** convertToMp3 is called
- **THEN** Exception is raised

### Requirement: FetchArtifacts

> Implementation: `fetch_artifacts` in `bridge/harvester.py` · confidence: llm

The system SHALL fetches a list of artifacts from the api.

#### Scenario: ArtifactsFetchedSuccessfully
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** API is reachable and returns a list of artifacts
- **WHEN** fetchArtifacts is called
- **THEN** List of artifacts is returned

#### Scenario: APIIsUnreachable
- **GIVEN** API is unreachable
- **WHEN** fetchArtifacts is called
- **THEN** Exception is raised

### Requirement: ProcessArtifact

> Implementation: `process_artifact` in `bridge/harvester.py` · confidence: llm

The system SHALL processes an artifact into an mp3 file and updates the history.

#### Scenario: ArtifactProcessedSuccessfully
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid artifact and history dictionary
- **WHEN** processArtifact is called
- **THEN** Artifact is processed into an MP3 file and history is updated

#### Scenario: ArtifactProcessingFails
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Invalid artifact or history dictionary
- **WHEN** processArtifact is called
- **THEN** Exception is raised

### Requirement: PurgeOldEpisodes

> Implementation: `purge_old_episodes` in `bridge/harvester.py` · confidence: llm

The system SHALL purges episodes older than retention_days from the history and filesystem.

#### Scenario: OldEpisodesPurgedSuccessfully
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** History dictionary with old episodes
- **WHEN** purgeOldEpisodes is called
- **THEN** Old episodes are removed from history and filesystem

#### Scenario: NoOldEpisodesToPurge
- **GIVEN** History dictionary with no old episodes
- **WHEN** purgeOldEpisodes is called
- **THEN** No changes are made

### Requirement: RebuildFeed

> Implementation: `rebuild_feed` in `bridge/harvester.py` · confidence: llm

The system SHALL rebuilds the podcast feed xml file from the history of episodes.

#### Scenario: FeedRebuiltSuccessfully
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid history dictionary
- **WHEN** rebuildFeed is called
- **THEN** Podcast feed XML file is updated

#### Scenario: FeedRebuildFails
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Invalid history dictionary
- **WHEN** rebuildFeed is called
- **THEN** Exception is raised

### Requirement: RunCycle

> Implementation: `run_cycle` in `bridge/harvester.py` · confidence: llm

The system SHALL runs a complete cycle of fetching artifacts, processing them, purging old episodes, and rebuilding the feed.

#### Scenario: CycleRunsSuccessfully
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid history dictionary
- **WHEN** runCycle is called
- **THEN** Artifacts are fetched, processed, old episodes are purged, and feed is rebuilt

#### Scenario: CycleFails
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Invalid history dictionary or API unreachable
- **WHEN** runCycle is called
- **THEN** Exception is raised

### Requirement: Main

> Implementation: `main` in `bridge/harvester.py` · confidence: llm

The system SHALL main entry point that loads history, runs a cycle, and saves history.

#### Scenario: MainRunsSuccessfully
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** History file exists or can be created
- **WHEN** main is called
- **THEN** History is loaded, cycle is run, and history is saved

#### Scenario: MainFails
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** History file cannot be loaded or saved
- **WHEN** main is called
- **THEN** Exception is raised

## Technical Notes

- **Dependencies**: Podcast, Episode, Media
