# Artifact Specification

> Generated by spec-gen v1.0.0 on 2026-04-24 22:59
> Source files: /Users/laurent/Projets/NoteCast/bridge/harvester.py

## Purpose

Defines core data models: Artifact, Episode, Podcast and 1 more.

## Entities

### Artifact

> `/Users/laurent/Projets/NoteCast/bridge/harvester.py`

Represents a media file that needs processing.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the artifact |
| url | string | URL where the artifact is located |
| type | string | Type of the artifact (e.g., audio, video) |

**Relationships:**

- belongs to Episode (Artifact is associated with an episode)

### Episode

> `/Users/laurent/Projets/NoteCast/bridge/harvester.py`

Represents an episode in a podcast.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the episode |
| title | string | Title of the episode |
| description | string | Description of the episode |
| created_at | datetime | Creation timestamp of the episode |

**Relationships:**

- belongs to Podcast (Episode belongs to a podcast)
- has-one Media (Episode has associated media)

### Podcast

> `/Users/laurent/Projets/NoteCast/bridge/harvester.py`

Represents a podcast.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the podcast |
| title | string | Title of the podcast |
| description | string | Description of the podcast |

**Relationships:**

- has-many Episode (Podcast has many episodes)

### Media

> `/Users/laurent/Projets/NoteCast/bridge/harvester.py`

Represents media associated with an episode.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the media |
| path | string | File path to the media |
| size | integer | Size of the media file in bytes |

**Relationships:**

- belongs to Episode (Media belongs to an episode)

## Requirements

### Requirement: ArtifactValidation

The system SHALL validate Artifact according to these rules:
- URL must be valid and accessible
- Type must be one of the supported formats

#### Scenario: ProcessArtifact
- **GIVEN** An artifact with a valid URL
- **WHEN** process_artifact() is called
- **THEN** The artifact is downloaded and converted to MP3 format
- **AND** A temporary file is created for processing
- **AND** The artifact is logged for tracking

### Requirement: EpisodeValidation

The system SHALL validate Episode according to these rules:
- Title must not be empty
- Description must not be empty
- Created_at must be a valid datetime

#### Scenario: PurgeOldEpisodes
- **GIVEN** Episodes older than a certain threshold
- **WHEN** purge_old_episodes() is called
- **THEN** Old episodes are removed from the history
- **AND** Associated media files are deleted
- **AND** The operation is logged

### Requirement: PodcastValidation

The system SHALL validate Podcast according to these rules:
- Title must not be empty
- Description must not be empty

#### Scenario: RebuildFeed
- **GIVEN** A podcast with episodes
- **WHEN** rebuild_feed() is called
- **THEN** The podcast feed is regenerated
- **AND** Episode media files are checked for existence
- **AND** The feed is written to the specified path
- **AND** The operation is logged

### Requirement: MediaValidation

The system SHALL validate Media according to these rules:
- Path must be a valid file path
- Size must be a positive integer

#### Scenario: CheckMediaExistence
- **GIVEN** An episode with associated media
- **WHEN** rebuild_feed() is called
- **THEN** The media file is checked for existence
- **AND** The media file size is verified
- **AND** The operation is logged
