# User Specification

> Generated by spec-gen v1.0.0 on 2026-04-30 11:33
> Source files: bridge/rss_transformer.py, bridge/harvester.py

## Purpose

Defines core data models: User, Job, Podcast and 1 more.

## Entities

### User

> `bridge/rss_transformer.py`

Represents a user configuration for the RSS transformer service.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Unique identifier for the user |
| db_path | Path | Path to the user's database file |
| public_dir | Path | Directory where public files are stored |
| base_url | string | Base URL for the user's resources |

**Relationships:**

- has-many Feed (User can have multiple feeds configured)
- has-many Job (User can have multiple processing jobs)

### Job

> `bridge/rss_transformer.py`

Represents a processing job for transforming an episode.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the job |
| status | string | Current status of the job |
| created_at | datetime | Timestamp when the job was created |
| updated_at | datetime | Timestamp when the job was last updated |

**Relationships:**

- belongs to User (Job belongs to a specific user)
- belongs to Episode (Job processes a specific episode)

### Podcast

> `bridge/harvester.py`

Represents a podcast with metadata and episodes.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| title | string | Title of the podcast |
| description | string | Description of the podcast |
| image_url | string | URL of the podcast's image |
| rss_file | Path | Path to the podcast's RSS file |

**Relationships:**

- has-many Episode (Podcast can have multiple episodes)

### Media

> `bridge/harvester.py`

Represents media content for an episode.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| url | string | URL of the media file |
| type | string | Type of the media file (e.g., audio/mpeg) |
| size | int | Size of the media file in bytes |

**Relationships:**

- belongs to Episode (Media belongs to an episode)

## Requirements

### Requirement: UserValidation

The system SHALL validate User according to these rules:
- User must have a unique name
- db_path must be a valid file path
- public_dir must be a valid directory path

#### Scenario: CreateUserConfiguration
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Valid user configuration parameters
- **WHEN** User is created
- **THEN** User object is initialized with provided parameters

### Requirement: JobValidation

The system SHALL validate Job according to these rules:
- Job must have a unique ID
- Status must be one of: 'pending', 'processing', 'completed', 'failed'
- created_at must be a valid datetime
- updated_at must be a valid datetime

#### Scenario: CreateProcessingJob
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** New episode detected
- **WHEN** create_job() is called
- **THEN** New job is created with 'pending' status

#### Scenario: ProcessJob
<!-- spec-gen-test: tags=regression (auto) -->
- **GIVEN** Job with 'pending' status
- **WHEN** process_job() is called
- **THEN** Job status is updated to 'processing' and eventually to 'completed' or 'failed'

### Requirement: PodcastValidation

The system SHALL validate Podcast according to these rules:
- RSS file must be writable
- Image URL must be valid

#### Scenario: PodcastFeedGeneration
- **GIVEN** Podcast with episodes
- **WHEN** rebuild_feed() is called
- **THEN** RSS feed is generated with all episodes

### Requirement: MediaValidation

The system SHALL validate Media according to these rules:
- URL must be valid
- Size must be positive

#### Scenario: MediaConversion
- **GIVEN** Source media file
- **WHEN** remux_to_m4a() is called
- **THEN** Media file is converted to M4A format
