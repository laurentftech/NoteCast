# API Specification

> Generated by spec-gen v1.0.0 on 2026-04-30 11:33

## Purpose

This document specifies the HTTP API exposed by the system.

## Requirements

### Requirement: APIAuthentication

The API SHALL require authentication via: None (internal service), API Key

#### Scenario: AuthenticatedRequest
- **GIVEN** a request with valid authentication credentials
- **WHEN** the request is processed
- **THEN** the request is authenticated successfully

#### Scenario: UnauthenticatedRequest
- **GIVEN** a request without authentication
- **WHEN** accessing a protected endpoint
- **THEN** the response status is 401 Unauthorized

### Requirement: POSTFeed

The API SHALL support `POST /api/feeds/poll` to poll rss feeds for new episodes and create processing jobs.

**Request:**

```json
{
  "user": "User"
}
```

**Response:**

```json
{
  "jobsCreated": "integer"
}
```

#### Scenario: PollFeeds
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Configured user with feeds
- **WHEN** POST /api/feeds/poll is called
- **THEN** 200 OK with count of created jobs

### Requirement: POSTFeed

The API SHALL support `POST /api/feeds/rebuild/{feedName}` to rebuild the rss feed xml from completed jobs.

**Request:**

```json
{
  "user": "User",
  "feedName": "string",
  "feedTitle": "string"
}
```

**Response:**

```json
{
  "success": "boolean"
}
```

#### Scenario: RebuildFeed
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** User with completed jobs
- **WHEN** POST /api/feeds/rebuild/{feedName} is called
- **THEN** 200 OK with success status

### Requirement: GETJob

The API SHALL support `GET /api/jobs/pending` to get the next pending processing job for a user.

**Request:**

```json
{
  "user": "User"
}
```

**Response:**

```json
{
  "job": "Job | null"
}
```

#### Scenario: GetPendingJob
- **GIVEN** User with pending jobs
- **WHEN** GET /api/jobs/pending is called
- **THEN** 200 OK with job details or null if none

### Requirement: PATCHJob

The API SHALL support `PATCH /api/jobs/{jobId}` to update a processing job's status and fields.

**Request:**

```json
{
  "fields": "object"
}
```

**Response:**

```json
{
  "success": "boolean"
}
```

#### Scenario: UpdateJob
<!-- spec-gen-test: tags=smoke (auto) -->
- **GIVEN** Existing job
- **WHEN** PATCH /api/jobs/{jobId} is called
- **THEN** 200 OK with success status

### Requirement: GETJob

The API SHALL support `GET /api/jobs/done/{feedName}` to get all completed jobs for a specific feed.

**Request:**

```json
{
  "user": "User",
  "feedName": "string"
}
```

**Response:**

```json
{
  "jobs": "Job[]"
}
```

#### Scenario: GetDoneJobs
- **GIVEN** User with completed jobs
- **WHEN** GET /api/jobs/done/{feedName} is called
- **THEN** 200 OK with array of completed jobs

### Requirement: GETConfig

The API SHALL support `GET /api/config` to load the transformer configuration.

**Response:**

```json
{
  "config": "object"
}
```

#### Scenario: LoadConfig
- **GIVEN** Valid config file exists
- **WHEN** GET /api/config is called
- **THEN** 200 OK with configuration object

### Requirement: GETEpisode

The API SHALL support `GET /api/episodes/fetch` to fetch episodes from an rss feed url.

**Request:**

```json
{
  "url": "string"
}
```

**Response:**

```json
{
  "feedTitle": "string",
  "episodes": "Episode[]"
}
```

#### Scenario: FetchEpisodes
- **GIVEN** Valid RSS feed URL
- **WHEN** GET /api/episodes/fetch is called
- **THEN** 200 OK with feed title and episodes

### Requirement: GETEpisode

The API SHALL support `GET /api/episodes/seen` to check if an episode has been processed before.

**Request:**

```json
{
  "user": "User",
  "episodeUrl": "string"
}
```

**Response:**

```json
{
  "seen": "boolean"
}
```

#### Scenario: CheckEpisodeSeen
- **GIVEN** User and episode URL
- **WHEN** GET /api/episodes/seen is called
- **THEN** 200 OK with seen status

### Requirement: GETEpisode

The API SHALL support `GET /api/episodes` to list all episodes in the podcast.

**Response:**

```json
{
  "episodes": "array"
}
```

#### Scenario: ListEpisodes
- **GIVEN** Episodes exist in the podcast
- **WHEN** GET /api/episodes is called
- **THEN** 200 OK with episodes array in response body

### Requirement: GETEpisode

The API SHALL support `GET /api/episodes/{episodeId}` to retrieve details of a specific episode.

**Request:**

```json
{
  "episodeId": "string"
}
```

**Response:**

```json
{
  "episode": "object"
}
```

#### Scenario: GetEpisode
- **GIVEN** A valid episode ID
- **WHEN** GET /api/episodes/{episodeId} is called
- **THEN** 200 OK with episode details in response body

### Requirement: GETService

The API SHALL support `GET /api/health` to check the health status of the harvester service.

**Response:**

```json
{
  "status": "string",
  "version": "string"
}
```

#### Scenario: HealthCheck
- **GIVEN** The harvester service is running
- **WHEN** GET /api/health is called
- **THEN** 200 OK with status and version in response body

### Requirement: POSTUser

The API SHALL support `POST /api/harvest` to trigger the harvesting process for a specific user.

**Request:**

```json
{
  "userId": "string"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string"
}
```

#### Scenario: HarvestUser
- **GIVEN** A valid user ID
- **WHEN** POST /api/harvest is called with userId in request body
- **THEN** 200 OK with status and message in response body

### Requirement: GETPodcast

The API SHALL support `GET /api/feed` to retrieve the generated podcast feed.

**Response:**

```json
{
  "feed": "string"
}
```

#### Scenario: GetFeed
- **GIVEN** A generated podcast feed exists
- **WHEN** GET /api/feed is called
- **THEN** 200 OK with feed in response body

### Requirement: POSTWebhook

The API SHALL support `POST /api/webhook` to trigger a webhook notification.

**Request:**

```json
{
  "event": "string",
  "data": "object"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string"
}
```

#### Scenario: TriggerWebhook
- **GIVEN** A valid event and data
- **WHEN** POST /api/webhook is called with event and data in request body
- **THEN** 200 OK with status and message in response body
